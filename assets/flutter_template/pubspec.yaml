name: vulpes-data
description: Lib which contains some extracted data from the game Minecraft
publish_to: 'none'

version: $version

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: "^3.35.7"

dependencies:
  flutter:
    sdk: flutter